---
description: "Scaffold a .claude/ directory and CLAUDE.md in the current project. Equivalent to `rose init`. Pass --force to wipe and reinitialise."
allowed-tools: Bash
---

Run `rose init $ARGUMENTS` in the current project directory and report what was created.
